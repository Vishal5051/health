const HeroSection = () => (

  <section className="hero">
    <h1>Welcome to SmartCare Hospital</h1>
    <p>Committed to your health, always.</p>
    <button className="cta-button">Book an Appointment</button>
  </section>
);


export default HeroSection;
